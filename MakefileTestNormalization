CC = gcc

TEST_FILE = test.c
TEST_EXEC = test
SRC_FILES = utils.c binary_operators.c 

all: test_bin_operators
rebuild: clean all

test_bin_operators:
	$(CC) $(TEST_FILE) $(SRC_FILES) -o $(TEST_EXEC)
	./$(TEST_EXEC)

clean:
	rm -rf $(TEST_EXEC)
